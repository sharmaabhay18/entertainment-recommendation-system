const usersData = require('./users');
const moviesData = require('./recommendedMovies');
const commentsData = require('./comments');

module.exports = {
  users: usersData,
  movies: moviesData,
  comments: commentsData,
};
